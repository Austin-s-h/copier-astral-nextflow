name: nextflow-runtime
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - python={{ python_version }}
  - nextflow
  - openjdk=17
  - pip
{% if include_bio_defaults %}
  - seqkit
  - samtools
{% endif %}
